/** Shared dashboard popup / form field typography. */

/** Field labels (inputs, selects, status, multi-select). */
export const POPUP_LABEL_CLASS = "form-label";

/** Section headings inside popups (e.g. English / Arabic). */
export const POPUP_SECTION_CLASS = "fs-16 fw-medium fnt-color mb-2";

/** Helper / subtext under labels. */
export const POPUP_HELP_CLASS = "small text-secondary mb-0";

/** Standard text inputs. */
export const POPUP_CONTROL_CLASS =
  "form-control form-control-lg textarea-hover-dark text-secondary";

/** Standard selects. */
export const POPUP_SELECT_CLASS = "form-select form-select-lg text-secondary";
